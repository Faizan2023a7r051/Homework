#include<stdio.h>
int main()
{
int i,j,sum=0;
printf("Enter the i value for natural number:");
scanf("%d",&i);
{
for(j=1:i<=i;j++)
sum=sum+j;
printf("the sum of%d natural number is %d",i,j);
return 0;
}
}
